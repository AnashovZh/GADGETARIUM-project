server:
  port: 4123
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: postgres
    url: jdbc:postgresql://localhost:5432/gadgetarium
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jwt:
    secret_key: java-10